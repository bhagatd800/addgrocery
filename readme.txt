- install node.js https://nodejs.org/dist/v8.9.1/node-v8.9.1-x64.msi
- in folder populateMongo run "npm install"
- in same folder run "node index"

data for collection must be in folder "new-data" with name "new-data.json"
in folder config lies config.js where configuretion of mongodb